#include "main.h"
/**
 * print_last_digit -print the last digit
 * @n: The last digit
 * Return: the last digit
 */
int print_last_digit(int n)
{
	int ld = n % 10;

	if (ld < 0)
		ld *= -1;
	
	_putchar(ld);
	
	else 

	return (ld);
}
