#include "main.h"
/**
 * main - check the code.
 *
 * Return: Always 0.
 */
void print_alphabet_x10(void)
{
	char la
	for (la = 'a'; la <= '10'; la <= 'z'; la++)

	{
	_putchar(la); }

	_putchar('\n')
	return (0);
}
